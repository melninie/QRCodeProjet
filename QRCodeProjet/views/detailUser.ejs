<!doctype html>
<html>
<head>
    <title>Détail Utilisateur</title>
    <% include layout %>
</head>

<body>
    <div class="container">
        <div class="col-sm-6 col-sm-offset-3">
            <h1><span class="fa fa-sign-in"></span>Formulaire de modification</h1>

            <form id="form">
                <div class="form-group">
                    <label>Nom</label>
                    <input type="text" class="form-control" name="nom" value="<%=(user[0].nomU.toUpperCase())%>">
                </div>
                <div class="form-group">
                    <label>Prénom</label>
                    <input type="text" class="form-control" name="prenom" value="<%=(user[0].prenomU)%>">
                </div>
                <div class="form-group">
                    <label>Mail</label>
                    <input type="text" class="form-control" name="mail" value="<%=(user[0].mailU)%>">
                </div>
                <% if((user[0].roleU)=='ETUDIANT') { %>
                <div class="form-group">
                    <label>Promotion</label>
                    <select name="promotion">
                        <% for(var i=0; i<promos.length; i++) {%>
                        <option value="<%=promos[i].idP%>" <% if(promos[i].idP==user[0].promotionU) { %> selected="selected" <% } %> ><%=promos[i].nomP%></option>
                        <% } %>
                    </select>
                </div>
                <% } %>

                <div class="pull-right">
                    <a href="/<%= chemin %>" class="btn btn-secondary btn-lg">Annuler</a>
                    <a href="/<%= chemin %>" class="btn btn-primary btn-lg" onclick="modifyItem('<%= user[0].id %>', '<%= chemin %>')">Appliquer</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>





